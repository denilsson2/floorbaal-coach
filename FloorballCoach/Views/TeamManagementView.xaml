<UserControl x:Class="FloorballCoach.Views.TeamManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FloorballCoach.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel - Add Team -->
        <Border Grid.Column="0" Background="#F5F5F5" Padding="20">
            <StackPanel>
                <TextBlock Text="Laghantering" 
                          FontSize="24" 
                          FontWeight="Bold" 
                          Margin="0,0,0,20"/>

                <TextBlock Text="Skapa nytt lag" 
                          FontSize="18" 
                          FontWeight="SemiBold" 
                          Margin="0,0,0,15"/>

                <TextBlock Text="Lagnamn *" Margin="0,0,0,5"/>
                <TextBox Text="{Binding NewTeamName, UpdateSourceTrigger=PropertyChanged}" 
                        Padding="8" 
                        FontSize="14"
                        Margin="0,0,0,15"/>

                <TextBlock Text="Kortnamn" Margin="0,0,0,5"/>
                <TextBox Text="{Binding NewTeamShortName, UpdateSourceTrigger=PropertyChanged}" 
                        Padding="8" 
                        FontSize="14"
                        Margin="0,0,0,15"/>

                <TextBlock Text="Lagfärg" Margin="0,0,0,5"/>
                <TextBox Text="{Binding NewTeamColor, UpdateSourceTrigger=PropertyChanged}" 
                        Padding="8" 
                        FontSize="14"
                        Margin="0,0,0,20"/>

                <Button Content="Skapa lag" 
                       Command="{Binding AddTeamCommand}"
                       Background="#4CAF50"
                       Foreground="White"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Padding="15,10"
                       BorderThickness="0"
                       Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" 
                                               CornerRadius="5"
                                               Padding="{TemplateBinding Padding}">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#45A049"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#CCCCCC"/>
                                    <Setter Property="Cursor" Value="Arrow"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>

                <TextBlock Text="Aktivt lag" 
                          FontSize="18" 
                          FontWeight="SemiBold" 
                          Margin="0,30,0,10"/>
                
                <Border Background="White" 
                       BorderBrush="#E0E0E0" 
                       BorderThickness="1" 
                       CornerRadius="5"
                       Padding="15">
                    <StackPanel>
                        <TextBlock Text="{Binding CurrentTeamName}" 
                                  FontSize="20"
                                  FontWeight="Bold"
                                  Foreground="#2196F3"
                                  TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding SelectedTeam.ShortName, StringFormat='({0})'}" 
                                  FontSize="14"
                                  Foreground="#666"
                                  Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- Right Panel - Team List -->
        <Border Grid.Column="1" Padding="20">
            <DockPanel>
                <TextBlock Text="Alla lag" 
                          FontSize="20" 
                          FontWeight="Bold" 
                          DockPanel.Dock="Top"
                          Margin="0,0,0,15"/>

                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Teams}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" 
                                       BorderBrush="#E0E0E0" 
                                       BorderThickness="1" 
                                       CornerRadius="5" 
                                       Margin="0,0,0,10"
                                       Padding="15">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" Cursor="Hand">
                                            <StackPanel.InputBindings>
                                                <MouseBinding MouseAction="LeftClick" 
                                                            Command="{Binding DataContext.SelectTeamCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                            CommandParameter="{Binding}"/>
                                            </StackPanel.InputBindings>
                                            
                                            <TextBlock Text="{Binding Name}" 
                                                      FontSize="18" 
                                                      FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding ShortName, StringFormat='Kortnamn: {0}'}" 
                                                      FontSize="12" 
                                                      Foreground="#666"
                                                      Margin="0,5,0,0"/>
                                            <TextBlock Text="{Binding CreatedDate, StringFormat='Skapat: {0:yyyy-MM-dd}'}" 
                                                      FontSize="12" 
                                                      Foreground="#999"
                                                      Margin="0,5,0,0"/>
                                        </StackPanel>

                                        <Button Grid.Column="1"
                                               Content="Ta bort"
                                               Command="{Binding DataContext.DeleteTeamCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                               CommandParameter="{Binding}"
                                               Background="#F44336"
                                               Foreground="White"
                                               Padding="10,5"
                                               BorderThickness="0"
                                               Cursor="Hand">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border Background="{TemplateBinding Background}" 
                                                                       CornerRadius="3"
                                                                       Padding="{TemplateBinding Padding}">
                                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                </Border>
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                    <Style.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Background" Value="#D32F2F"/>
                                                        </Trigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
